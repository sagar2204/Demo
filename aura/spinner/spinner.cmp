<aura:component>
    <aura:attribute name="issearching"    type="Boolean" default="false"/>

    <div >
        <lightning:input
            aura:id="enter-search"
            name="enter-search"
            label="Search when user hits the 'enter' key"
            isLoading="{! v.issearching }"
            type="search" onchange="{! c.handleKeyUp }" 
        /> 
    </div>

</aura:component>